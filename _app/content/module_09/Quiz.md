# Module 9 Quiz: Summary Queries

**Instructions:** Answer the following questions. Each question is worth 4 points (total: 20 points).

1. What does the HAVING clause do that WHERE cannot?
2. When should you use a window function instead of GROUP BY?
3. Give an example of a query that uses `AVG()` and `COUNT()` together.
4. What does `PARTITION BY` do in a window function?
5. How can you calculate a running total using SQL?

---

**Submit your answers in a file named `LastnameFirstnameModule9Quiz.txt`.**