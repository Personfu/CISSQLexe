# Module 14: How to Use Transactions and Locking

## Module Overview
This module teaches how to use transactions and locking to make MySQL changes safe, consistent, and recoverable.

### Learning Outcomes
- Write transaction-safe stored procedures
- Use `COMMIT` and `ROLLBACK`
- Lock selected rows to prevent concurrent conflicts
- Use `LAST_INSERT_ID()` to connect related inserts
