# Module 14 Quiz: Transactions and Locking

**Instructions:** Answer the following questions. Each question is worth 4 points (total: 20 points).

1. What does `START TRANSACTION` do?
2. When should you use `ROLLBACK` instead of `COMMIT`?
3. How does `FOR UPDATE NOWAIT` affect a selected row?
4. Why is row locking important when merging customer records?
5. What is the purpose of `LAST_INSERT_ID()` in transactional code?

---

**Submit your answers in a file named `LastnameFirstnameModule14Quiz.txt`.**
