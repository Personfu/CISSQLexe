# Final Project Quiz

**Instructions:** Answer the following questions to confirm your understanding of the final project requirements.

1. Why should `utf8mb4` be used for new MySQL databases?
2. What does `ENGINE=InnoDB` provide that MyISAM does not?
3. Why are foreign keys important in the `downloads` table?
4. What does `WITH CHECK OPTION` do when used in a view?
5. Why is it useful to include `DROP DATABASE IF EXISTS` in a script?

---

**Save your answers in a file named `FinalProject_QuizAnswers.txt`.**