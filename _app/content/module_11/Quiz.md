# Module 11 Quiz: Data Types

**Instructions:** Answer the following questions. Each question is worth 4 points (total: 20 points).

1. What is the difference between `VARCHAR` and `CHAR`?
2. When should you use `DECIMAL` instead of `FLOAT`?
3. Which MySQL type would you choose for a timestamp with time zone information?
4. What does `NOT NULL` enforce in a column definition?
5. Why is it important to choose the correct length for text fields?

---

**Submit your answers in a file named `LastnameFirstnameModule11Quiz.txt`.**