# Module 10 Quiz: Subqueries

**Instructions:** Answer the following questions. Each question is worth 4 points (total: 20 points).

1. What is the difference between a scalar subquery and a table subquery?
2. How does a correlated subquery differ from a regular subquery?
3. When would you use `EXISTS` instead of `IN`?
4. What does `ALL` mean in a subquery comparison?
5. Give a simple example of a query using a subquery in the `WHERE` clause.

---

**Submit your answers in a file named `LastnameFirstnameModule10Quiz.txt`.**